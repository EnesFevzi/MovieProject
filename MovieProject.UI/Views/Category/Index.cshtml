@model List<ResultCategoryDto>

<table id="hospitalTable" class="table table-bordered">
    <thead>
        <tr>
            <th>Kategori Adı</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td><a asp-action="Update" asp-controller="Category" asp-route-id="@item.CategoryId" class="btn mb-1 btn-rounded btn-outline-info">Güncelle</a></td>
                <td><a asp-action="Delete" asp-controller="Category" asp-route-id="@item.CategoryId" class="btn mb-1 btn-rounded btn-outline-danger">Sil</a></td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="Create" asp-controller="Category" class="btn mb-1 btn-rounded btn-outline-info">Yeni Kategori Ekle</a>


